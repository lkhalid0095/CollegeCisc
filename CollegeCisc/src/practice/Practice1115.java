package practice;

import java.util.Arrays;

public class Practice1115 {

	/* Reversing the elements of an array involves swapping the corresponding elements of the array: the 	       
	 * first with the last, the second with the next to the last, and so on, all the way to the middle of the 
	 * array.
	 * Given an array a and two other int variables, k and temp, write a loop that reverses the elements of 
	 * the array.
	 * Do not use any other variables besides a, k, and temp.
	 */
	public static void main(String[] args) {
		int[] a = {1,2,3,4,5,6};
		int k;
		for( k = 0; k < a.length; k++){
		    
		    
		    }
		System.out.println(Arrays.toString(a));
	}

}
